# Sprint 11 — 从网站到微信群：一键长图、卖家摊位、统一问询栏 · 规格草稿（2026-09-18）

> **状态：草稿，未拍板。** 由 2026-09-18 与 Sean 的构思对话整理。
> 每一条都标了来源：**【Sean】**= 他明确说的；**【建议】**= AI 提的、他没反对但也没确认；**【猜测】**= AI 自己填的空，最需要他否决；**【事实】**= 查代码得到的现状。
> 取舍原则【Sean】：体验做到极致丝滑，工程为体验服务；滥用靠埋点侦测 + 分级提醒兜底，不靠预先加限制。方案 AI 可以建议，取舍由人感受后拍板。

## 要解决的痛点【Sean】

卖家在网站上发完物品，要再搬到微信二手群，中间有摩擦。理想体验：在「我的」里点一下，得到一张包含自己全部在售物品的长图，直接贴到群里。群友扫码进站看详情，进站后能顺滑地逛到全站其他内容 —— 卖家发图等于在替平台打广告。

## 已拍板的决定【Sean】

1. 长图是确定性渲染：每件物品的照片、标题、关键信息原样取自帖子。**不用 AI 改写文案**（AI 出文本信任度低）。用不用 AI 不重要。
2. 长图带**一个**二维码，指向「这位卖家的全部在售」。
3. **不做手打短编号**。手打是很差的体验。
4. 扫码进站的本质 = 完成一次「按卖家搜索」。进站后要有丝滑的入口回到 / 探索全站，并在下面推荐其他物品：以该卖家的物品作为搜索引子，复用现有搜索逻辑找匹配物品。
5. 允许在搜索框里按联系方式（如微信号）搜某卖家的全部物品，不视为隐私问题。
6. 动态防御：对过快的批量操作埋点侦测。轻 → 后台可见，周期性查看；重 → 发邮件到运营邮箱提醒处理。
7. 反馈入口：只在用户卡壳、需要问询时出现，不占用户的信息渠道。做进搜索栏 —— 搜索栏同时是搜索、AI、问运营的入口，**一个 UI**。视觉借用「AI 智能助手」惯用的炫彩风格，但要**轻量、静态**，不带走注意力。

Sean 说会自己测「长图里的链接在微信里能否直接点开」。【事实】图片是像素，微信不会把图中文字识别为可点链接；能跳转的只有「长按识别二维码」。所以链接不印在图上，二维码是唯一通道。

---

## 11A 卖家筛选（摊位）

**数据**
- 新表 `Shelf`：`slug`（短、不透明、唯一）、`contactValue`（唯一，只在服务端用）、`createdAt`。【建议】
- 同一卖家永远同一个 slug → 他发出去的所有长图都指向同一个**实时**摊位（新发的会出现，已售的消失），旧图不过期。【建议】
- 用户第一次点「一键生成长图」时创建，创建前用现有「联系方式 + 编辑码」校验（`POST /api/items/by-contact` 同款）。【建议】

**入口**
- `/s/<slug>` → 服务端跳转 `/?shelf=<slug>&utm_source=wx_poster`（同现有 `/listing/[id]` 的跳转模式）。顺带补上 STATE.md 里「微信来源归因丢失」的待办。【建议】
- 二维码里放 slug 不放微信号：个人信息不进 URL 是本项目既有规矩（日志、浏览记录、转发链路都会留 URL）；短码也让二维码更稀疏好扫。【建议】
- 搜索框直接输入联系方式 → 同一个筛选【Sean 决定 5】。【事实】`GET /api/items/by-contact?value=` 线上已有此能力（物品卡「同卖家其他物品」在用），返回不含联系方式，计入披露配额 `gateReveal`。

**页面行为**：不做单独卖家页，落在主页（沿用 `shareText.ts` 的「网站只有一个主页」原则）。【建议，Sean 以「本质是一次卖家搜索」认可】
1. 顶部导航、三站 tab 照常。
2. 搜索栏里出现一个筛选标签「这位卖家的 N 件 ✕」；点 ✕ 原地变回完整主页，不跳转不重载。显示名用卖家昵称（`hb_nickname`），没有则「这位卖家」。【猜测：昵称目前只存在发布者本地，服务端未必有，可能需要生成长图时一并提交】
3. 筛选结果：用主页同款物品卡，展开 / 询价 / 购物车行为一致。
4. 之后接「和这些相似的」，再接全站物品流，不设页面到底。

## 11B 相似物品延伸

- 对卖家每件物品已存的向量（10A）分别查最近邻（`VectorStore.nearest`），排除卖家自己的，合并去重，取前 12。【建议】
- **按件分别查，不取平均**：卖家的东西通常很杂（沙发 + 显示器），平均向量什么都不像。【建议】
- 不调大模型、不调 embedding 接口，只是数据库查询：零费用，不占搜索配额。【事实：向量已在库里】
- 向量不可用时兜底「同类目最新」。【建议】
- 呈现方式复用 10B「关键词结果后补语义结果」的那一段 UI。【建议】

## 11C 长图渲染与生成按钮

- 「我的」当前 tab 下一个按钮「一键生成长图」【Sean】。v1 只做二手 tab；室友、活动停放。【猜测】
- 服务端渲染出真实 PNG 地址（复用 `@vercel/og` 与活动 OG 卡的中文字体方案）。必须服务端：微信内置浏览器里只能长按 `<img>` 保存，canvas 下载不可用。【事实 + 建议】
- 版式【建议】：顶部卖家一句话标题；每件一行卡片（左照片，右标题 / 价格 / 关键信息）；底部二维码 +「长按识别二维码 · 看详情、联系卖家」+ 站名。照片走 Cloudinary 缩略图变换控体积。
- 物品多时按件数拆成多张（暂定每张 ≤ 10 件），每张都带二维码。【猜测：阈值要真机发群里看压缩效果再定】
- 出图接口按物品 id 取数，图里只有公开信息，不需要新鉴权。【建议】

## 11D 异常侦测与分级提醒

【事实】已有：`RateLimitHit` 逐次记录披露 / 按联系方式查询（谁、查谁、放行或被拒）；每日摘要 `digest.ts` 已有「24h 披露被拒 ≥ 50 次」告警；Resend 发信通道。

要补的：
- 新信号：同一访客短时间内查询了多少**不同**卖家（放行的也算）。现在只数被拒次数，控制在配额内的慢速抓取测不到。【建议】
- 分级【Sean 定级，数字是猜测，上线后按真实数据调】：
  - 轻：1 小时内 ≥ 10 位不同卖家 → 后台面板 + 每日摘要。
  - 重：1 小时内 ≥ 30 位，或同一 IP 换多个访客身份在查 → **立即**发邮件；同一事件 1 小时内只发一封。
- 有侦测兜底后，正常用户侧配额放松：扫码进来的首次查询不计数。【建议】
- **待定**：重级是只通知，还是同时自动处置（该访客配额降为零 24h）？【建议：先只通知，看过几次真实告警再定】
- **待定**：告警邮箱用新环境变量 `ALERT_EMAIL_TO`（不写进仓库，同 `DIGEST_EMAIL_TO` 的做法）。【建议】

## 11E 统一问询栏（可独立上线）

【Sean 决定 7】搜索、AI、问运营是一个栏，不是两个输入框。

**行为**【建议】
- 输入关键词 → 第 0 / 1 层照常（免费，不调模型）。
- 输入看起来是问句或描述（问号结尾、含「怎么 / 为什么 / 有没有 / how」等）→ 同一个栏下方就地展开对话区（10C 的 `SearchChat` 并入，不再有单独的「没找到？」输入框）。【猜测：问句判断规则；误判代价低，当关键词搜不到东西后仍可一键转对话】
- 关键词结果不满意 → 栏内一个「问 AI」动作，把当前词带进对话。【建议】
- 对话的配额、预算熔断、输出校验全部沿用 10C。

**问运营**【建议】
- AI **只分类不回答**：在现有那一次模型调用的 JSON 输出里加 `intent`（`find` / `ask_ops`），零额外调用。不让 AI 回答「网站怎么用」—— 答错更糟，且 Sean 对 AI 文本信任度低。
- `ask_ops` 时显示固定模板卡片：「这个问题我帮你转给站长」+ 用户原话（可改）+ 选填联系方式（预填 `getSharedContact()`）+ 发送。**必须用户点发送才提交**，对话不自动转发（与 10C「对话不落库」一致）。
- 改 `SYSTEM_PROMPT` 第 6 条：与找东西无关的请求不再一律拒绝，站务类归 `ask_ops`，闲聊类仍拒绝。
- 不依赖 AI：熔断 / AI 关闭时，在其他卡壳时刻（零结果、报错、触发配额提示）放一行普通文字「遇到问题？告诉站长」，打开同一张卡片。
- 新表 `Feedback`（内容、选填联系方式、来源页面、visitor、时间、状态）；进后台面板 + 每日摘要（轻量级提醒）。提交接口走 `checkQuota` 防刷。

**视觉**【建议，待 Sean 看实物】
- 原则：颜色只在用户注意力已经落到搜索栏时才出现。
- 未聚焦：与现在一样朴素；图标换成星芒（`Sparkles`，第 2 层已在用）静态渐变填色；占位文字「搜物品，或直接提问」。
- 聚焦：边框变 1px 渐变细线。
- 全程静态：无流光、无呼吸、无动效。一条 CSS 渐变，零 JS。

---

## 顺序与 PR 切分【建议】

| 子项 | 依赖 | 风险级（猜测） |
|---|---|---|
| 11E 统一问询栏 + 反馈 | 无，可先做 | 2（改公开对话接口的 prompt 与输出结构、新表） |
| 11A 卖家筛选 | 无 | 2（新表、新公开入口） |
| 11B 相似延伸 | 11A | 1 |
| 11D 侦测与提醒 | 11A（信号来源） | 2（发信、可能的自动处置） |
| 11C 长图 | 11A（二维码要有落点） | 1 |

每项独立分支、独立 PR，不叠 PR。

## 范围外（停放）

- AI 帮卖家整理 / 改写文案、AI 看图挑封面。
- 室友站、活动站的长图。
- AI 直接回答站务问题、FAQ。
- 每件物品各自的小二维码。

## 还没人回答的问题

1. 11D 重级：只通知 vs 自动处置。
2. 11D 告警邮箱走 `ALERT_EMAIL_TO`。
3. 11A 卖家显示名从哪来（昵称是否随生成长图提交到服务端）。
4. 11C 单张件数上限 —— 需要真机发群测试。
5. 11E 问句判断规则是否可接受；「问 AI」动作放在栏内什么位置。
