# Sprint 12 — 黑堡问答：知识库、AI 搜索、荣誉系统 · 规格草稿（2026-09-18）

> **状态：草稿，未拍板。** 由 2026-09-18 Sean 的一段需求描述整理。
> 每一条都标了来源：**【Sean】**= 他明确说的；**【建议】**= AI 提的、待他确认；**【猜测】**= AI 自己填的空，最需要他否决；**【事实】**= 查代码得到的现状。
> 取舍原则沿用 Sprint 11【Sean】：体验丝滑优先；滥用靠埋点侦测 + 分级提醒兜底，不靠预先加限制。

## 要解决的痛点【Sean】

新生有很多问题不知道去哪问。很多细节只有老生答得了，但乐于助人的老生没那么多。所以**同一个问题只该被老生回答一次**，之后所有人都能立刻查到。

## 已拍板的决定【Sean】

1. **知识库**：过去回答过的问题要能被迅速找到、迅速回答。
2. **AI 搜索**：帮用户在知识库里找内容，或者直接给出解答；没有解答就直接把问题发出去；有人回答了，在提问者的页面做提醒。
3. **提醒要做得更好**：用户在主页面的「我的」上就能看到提醒数量。
4. **荣誉系统**：采用知乎的逻辑。
5. 本质是一个社交平台，树洞、交友、问答都可以装进来。
6. **并入「黑堡」板块**，不加第四个选项卡（三个已经过多）。

## 三个最需要 Sean 否决的大猜测

| # | 猜测 | 为什么这么猜 | 否决的代价 |
|---|---|---|---|
| G1 | **身份 = 设备上的 `hb_vid` 自动生成一份「档案」（昵称 + 一句话身份，如「CS 博三」），不强制登录；想跨设备保留荣誉的人用现有 magic-link 绑邮箱** | 【事实】全站无登录，`hb_vid` 已是活动评论的作者标识；magic-link 目前「不保护任何资源」，正好给它一个用途。强制登录会直接砍掉老生的回答意愿 | 数据模型用独立 `Profile` 表 + 设备关联表，改成「回答必须绑邮箱」只是加一道门，不返工 |
| G2 | **AI 的回答作为帖子里的第一条回答落库，明确标「AI 回答 · 未经老生验证」；老生只需点「赞同」或写一句纠正** | 直接对准痛点：老生「点一下」比「从头写」省力得多，热心人少也转得动。被赞同的 AI 回答自动去掉「未验证」标 | 不落库则 AI 回答只在对话里一次性展示（与 10C 一致），知识库只收人写的内容 |
| G3 | **「交友」本期不单独做** | 【事实】STATE.md 记录用户自发的「找搭子」活动是全站最受欢迎内容，活动板块已经承担交友。再开一个入口会分流 | 以后在帖子类型里加一种即可 |

---

## 12A 问答基础（数据模型 + 发问 / 回答 / 赞同）

**入口**【建议】
- 「黑堡」页顶部加二级切换：`活动` | `问答`。路由 `/localnews/qa`（列表）、`/localnews/q/<id>`（详情，**SSR**）。选项卡数量不变【Sean 决定 6】。
- 详情页 SSR + `QAPage` JSON-LD + 进 sitemap。【建议】知识库最大的流量来源会是搜索引擎：新生搜「Blacksburg 怎么办驾照」应该能搜到本站。

**数据**【建议】
- `Profile`：`id`、`nickname`、`tagline`（一句话身份，选填）、`email?`（绑定后才有）、荣誉计数缓存、`createdAt`。
- `ProfileDevice`：`visitorId @unique → profileId`。一个档案多台设备；magic-link 登录时把当前设备并入邮箱对应的档案。
- `Thread`：`kind`（`question` | `talk`）、`topic`、`title`、`body`、`photoUrls`、`profileId`、`anonymous`、`status`（`open` | `solved` | `hidden` | `deleted`）、`answerCount`、`viewCount`、`bumpedAt`、向量三件套（同 10A）。
- `Answer`：`threadId`、`profileId`、`anonymous`、`isAi`、`body`、`upvotes`、`accepted`、`status`。
- `AnswerComment`：回答下的短评（≤300 字，同 `EventComment`）。
- `AnswerVote`：`@@unique(answerId, profileId)`，只有赞同，没有反对。【猜测：小社区里「反对」伤人多于有用；内容有问题走举报】
- 话题（`topic`）初始表【猜测】：入学报到 · 签证身份 · 住房 · 交通出行 · 选课学业 · 生活购物 · 医疗保险 · 银行税务 · 其他。

**行为**
- 发问：标题必填，正文 / 图片选填，可勾「匿名」。匿名时 `profileId` 照存（提醒与荣誉要用），**任何接口都不下发**。
- 回答按「已采纳 → 赞同数 → 时间」排序【知乎逻辑】。提问者可把一条回答标为「解决了我的问题」（→ `solved`）。【猜测：知乎没有采纳，但知识库需要一个「这条可信」的信号】
- 不收联系方式。问答是公开内容，不涉及 §5 披露面；想私下联系走活动板块现成的「发送联系方式」。【建议】
- 举报沿用 `Report`（3 个不同 IP 自动隐藏）；发问 / 回答 / 赞同的配额走 `checkQuota`；隐藏与删除清向量（同 10A 生命周期）。

## 12B 提醒中心

【事实】现有提醒是纯客户端的：`lib/notifications.ts` 靠 localStorage 里缓存的「联系方式 + 编辑码」去拉 by-contact 比时间戳，二手页和室友页各算各的，黑堡页没有徽标，换设备即失效。

- 新表 `Notification`：`profileId`、`type`（`answer` 有人回答我的问题 · `comment` · `upvote` 我的回答被赞同 · `accepted` · `invite` 被邀请回答）、`threadId` / `answerId`、`readAt`。同类合并：「3 人赞同了你的回答」是一条。【建议】
- `GET /api/my/unread` → `{ qa, total }`，按 `hb_vid → Profile` 鉴权。
- **三个页面的「我的」按钮共用一个徽标组件**，数字 = 问答未读 + 二手未读 + 室友未读【Sean 决定 3】。现有两路 localStorage 计数原样保留、并入合计，不改键名（不变量 §8.7）。
- 「我的」面板加「提醒」页签，列表点进去直达对应回答并标已读。
- 邮件【建议】：发问时一个默认勾选的选项「有人回答时发邮件告诉我」，填邮箱即走 magic-link 同一套 Resend。新生问完就走、不会回来刷页面，没有站外提醒这个闭环是断的。同一问题 1 小时内最多一封。

## 12C AI 搜索与「问不到就发出去」

复用 Sprint 10 的全部地基（`embedText` 白名单、`vectorStore`、`hybrid`、`chat` 的配额 / 预算 / 输出校验）与 11E 的统一问询栏（#25 合入后接）。

1. 用户在问答页的栏里输入问题 → 第 0 层关键词 + 第 1 层语义匹配 → 列出相似的已有问题（优先 `solved`、赞同多的）。
2. 「让 AI 回答」→ 取最相近的若干帖子及其高赞回答作为候选 → 模型给出整理后的答案，**每个论断带来源帖子**；服务端校验引用的 id 必须在候选集合内（不变量 §8.12 同款）。
3. 知识库没有覆盖时，AI 可用通用知识作答，但固定标注「AI 通用回答 · 未经老生验证」【Sean 决定 2 +建议】。
4. 答案下方固定两个动作：「解决了」/「还是没解决，发出去问老生」。后者打开发问框，标题与正文已由对话预填，用户点发送才提交（同 11E：对话不自动落库）。
5. 若 G2 成立：发出去时 AI 的回答随帖落库为第一条 `isAi` 回答。
6. 送入模型的只有标题、正文、话题、回答正文；昵称、档案、邮箱、visitorId 一律不进（不变量 §8.11 扩到新表）。

## 12D 荣誉系统（知乎逻辑）

- **声望**【猜测：权重】：回答被赞同 +10 · 回答被采纳 +15 · 提问被 3 人以上「同问」+5 · AI 回答被我纠正且纠正被采纳 +20。匿名发布照常计分，只是不展示出处。
- **等级称号**【猜测】：新生 → 热心同学 → 学长 / 学姐 → 黑堡通 → 黑堡活地图。称号跟在昵称后面。
- **话题徽章**：某话题下声望前列 →「住房 · 优秀回答者」【知乎逻辑】。
- **个人页** `/u/<id>`：昵称、一句话身份、称号、徽章、TA 的回答（不含匿名的）。
- **本月热心榜**：问答页侧栏 / 顶部一行，每月清零的榜单，长期声望不清。【建议】
- **邀请回答**【知乎逻辑 + 建议】：发问后系统按话题自动邀请该话题声望最高的几位（发 `invite` 提醒），提问者也可手动点人。这是把稀缺的热心老生精准路由到问题上的关键。每人每天被邀请数有上限。
- 防刷：不能赞自己；赞同按档案去重；异常（同 IP 多档案互赞）进 11D 的侦测信号，只通知不自动处置。

## 12E 树洞与冷启动

- 树洞 = `Thread.kind='talk'` + 默认匿名，问答页的一个筛选项，不计声望。【建议】
- 冷启动【建议】：后台加「导入问答」——贴一段微信群聊天记录，LLM 抽成「问题 + 回答」草稿，Sean 审过再发布（同 `BatchImportPanel` 的人审模式）。知识库空着上线，AI 搜索就没东西可搜。

---

## 顺序与 PR 切分【建议】

| 块 | 依赖 | 风险级 |
|---|---|---|
| 12A 问答基础 | 无 | 2（新表、新公开接口、配额） |
| 12B 提醒中心 | 12A | 2（新表、`hb_vid` 鉴权的接口、发信） |
| 12C AI 搜索 | 12A；11E（#25）合入 | 2（公开对话接口、embedding 白名单扩表） |
| 12D 荣誉系统 | 12A、12B（邀请走提醒） | 2 |
| 12E 树洞 + 导入 | 12A | 1–2 |

每块独立分支、从 main 起、不叠 PR。两份 schema 同步改（不变量 §8.8）。

## 范围外（停放）

- 交友（G3）、私信、关注 / 粉丝、信息流推荐算法。
- 室友站、二手站的提醒搬到服务端 `Notification`（本期只并入计数）。
- 反对票、折叠回答、专栏文章。

## 还没人回答的问题

1. 板块叫什么？「问答」/「问学长」/「黑堡百科」。【草稿暂用「问答」】
2. 老生身份要不要验证（如 vt.edu 邮箱 → 「VT 在读」标）？可信度 vs. 门槛。
3. AI 回答落库（G2）后，如果一直没人赞同也没人纠正，多久之后在列表里降权？
